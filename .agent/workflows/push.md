---
description: Auto push all changes to GitHub
---

// turbo-all
1. Run `git add -A` to stage all changes in the backend directory
2. Run `git commit -m "Auto push: update backend"` — if nothing to commit, skip
3. Run `git push origin master` to push to GitHub
