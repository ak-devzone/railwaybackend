# Backend Environment Variables for Vercel
# Copy these values into your Vercel project's Environment Variables dashboard
# (Settings → Environment Variables)

# ─── Django Core ───────────────────────────────────────────────────
SECRET_KEY=your-very-long-random-secret-key-here
DEBUG=False
ALLOWED_HOSTS=your-backend-project.vercel.app

# ─── Database (Railway MySQL) ───────────────────────────────────────
DB_ENGINE=django.db.backends.mysql
DB_NAME=railway
DB_USER=root
DB_PASSWORD=TnrmYuHKRLkiCNEXvDguSSGfQyOHRTRq
DB_HOST=shortline.proxy.rlwy.net
DB_PORT=40877

# ─── Frontend URL (for CORS) ────────────────────────────────────────
FRONTEND_URL=https://your-frontend-project.vercel.app
VERCEL_FRONTEND_URL=https://your-frontend-project.vercel.app

# ─── Firebase Admin SDK ─────────────────────────────────────────────
# Paste the ENTIRE contents of firebase-credentials.json as a single line below
# Run this in PowerShell to get it: (Get-Content firebase-credentials.json) -join '' | Set-Clipboard
FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# ─── Email (Gmail SMTP) ─────────────────────────────────────────────
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=Digital Library <noreply@digitallibrary.com>
ADMIN_EMAIL=admin@digitallibrary.com
